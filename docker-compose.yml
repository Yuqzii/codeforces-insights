services:
  server:
    build:
      context: ./backend
      dockerfile: Dockerfile.server
      args:
        - GO_BUILD_TAGS=profile
    restart: "unless-stopped"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    depends_on:
      migrate:
        condition: service_completed_successfully
    networks:
      default:
        aliases:
          - codeforces-insights_server

  fetcher:
    profiles: [fetcher]
    build:
      context: ./backend
      dockerfile: Dockerfile.fetcher
    restart: "no"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    depends_on:
      migrate:
        condition: service_completed_successfully

  postgres:
    image: postgres:17-alpine
    restart: "unless-stopped"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  migrate:
    build:
      context: ./backend
      dockerfile: Dockerfile.migrate
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    command: [
      "-database", "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable",
      "up"
    ]

  frontend:
    profiles: [frontend]
    build: ./frontend
    volumes:
      - ./frontend/public:/app/public:ro,Z
      - ./frontend/src:/app/src:ro,Z
    command: sh -c "npm run dev & npm run serve"
    ports:
      - "3000:3000" # Browsersync site, not meant to be used
      - "3001:3001" # Browsersync UI

  nginx:
    profiles: [frontend]
    build: ./nginx
    ports:
      - "443:443" # This is where you should visit the site for HTTPS and HTTP/2
    depends_on:
      - server
      - frontend

volumes:
  db:

networks:
  default:
    name: codeforces-insights_net
    driver: bridge
