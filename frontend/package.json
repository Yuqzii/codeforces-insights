{
  "devDependencies": {
    "esbuild": "0.25.10"
  },
  "scripts": {
	"dev": "esbuild src/main.js src/style.css --bundle --outdir=dist --sourcemap --watch=forever & cp -r public/* dist/",
    "prod": "esbuild src/main.js src/style.css --bundle -outdir=dist --minify"
  }
}
