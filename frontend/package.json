{
  "devDependencies": {
    "@eslint/css": "^0.14.1",
    "@eslint/js": "^9.39.2",
    "@html-eslint/eslint-plugin": "^0.55.0",
    "@html-eslint/parser": "^0.55.0",
    "browser-sync": "^3.0.4",
    "esbuild": "0.25.10",
    "eslint": "^9.39.2",
    "gaze": "^1.1.3",
    "globals": "^17.0.0"
  },
  "scripts": {
    "dev": "mkdir -p dist && node esbuild-config-dev.js",
    "serve": "node bs-config.js",
    "build": "node esbuild-config && cp -r public/* dist/",
    "lint": "npx eslint src/** public/**"
  }
}
